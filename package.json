{
  "name": "mw-dict",
  "version": "3.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib -p . && cp src/*.d.ts lib",
    "test": "jest",
    "snyk-protect": "snyk protect",
    "prepare": "yarn run snyk-protect"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "lodash": "^4.17.5",
    "xmldom": "^0.1.27",
    "snyk": "^1.316.1"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "micro": "^8.0.4",
    "yargs": "^8.0.2"
  },
  "snyk": true
}
