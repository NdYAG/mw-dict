{
  "name": "mw-dict",
  "version": "3.0.0",
  "main": "lib/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf lib && tsc --outDir lib -p . && cp src/*.d.ts lib",
    "test": "jest"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "lodash": "^4.17.21",
    "xmldom": "^0.1.27"
  },
  "devDependencies": {
    "jest": "^20.0.4",
    "micro": "^8.0.4",
    "yargs": "^8.0.2"
  }
}
